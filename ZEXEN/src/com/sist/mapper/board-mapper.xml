<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.board-mapper">
<!-- 
<insert id="boardInsert" parameterType="BoardVO">
   INSERT INTO board(board_no,member_id,subject,content,regdate,hit,like_cnt,hate_cnt,file_name,file_size,board_cate_no)
   VALUES(
     (SELECT NVL(MAX(board_no)+1,1) FROM board),
     #{member_id},
     #{subject},
     #{content},
     #{regdate},
     #{hit},
     #{like_cnt},
     #{hate_cnt},
     #{file_name},
     #{file_size},
     #{board_cate_no}
   )
</insert>
  -->
  
  <select id="boardListData" resultType="BoardVO" parameterType="hashmap">
   SELECT board_no,subject,member_id,regdate,hit,num
   FROM (SELECT board_no,subject,member_id,regdate,hit,rownum as num
   FROM (SELECT board_no,subject,member_id,regdate,hit 
   FROM board ORDER BY board_no DESC))
   WHERE num BETWEEN #{start} AND #{end}
  </select>
  
  <!--게시글 총갯수-->
  <select id="boardListCount" resultType="BoardVO" parameterType="hashmap">
   SELECT COUNT(*) FROM board;  
  </select>
  
  <select id="boardCateListData" resultType="BoardVO" parameterType="hashmap">
   SELECT board_no,subject,member_id,regdate,hit,num
   FROM (SELECT board_no,subject,member_id,regdate,hit,rownum as num
   FROM (SELECT board_no,subject,member_id,regdate,hit 
   FROM board WHERE board_cate_no=#{board_cate_no} ORDER BY board_no DESC))
   WHERE num BETWEEN #{start} AND #{end}
  </select> 
  
  <select id="boardTotalPage" resultType="int">
   SELECT CEIL(COUNT(*)/10.0) FROM board
  </select>
  
  <update id="boardhitIncrement" parameterType="int">
   UPDATE board SET
   hit=hit+1
   WHERE board_no=#{board_no}
  </update>

  <select id="boardDetailData" resultType="BoardVO" parameterType="int">
   SELECT board_no,member_id,subject,content,regdate,hit
   FROM board
   WHERE board_no=#{board_no}
  </select>


</mapper>